<Page xmlns="http://schemas.nativescript.org/tns.xsd" xmlns:avatar="components/avatar" navigatingTo="navigatingTo">
    <ActionBar title="My App" icon="" />

    <StackLayout class="p-20">
        <!-- here we need to get some data from the avatar component -->
        <avatar:avatar id="avatar" />
        <Label text="Tap the button" class="h1 text-center" />
        <Button text="TAP" tap="{{ onTap }}" class="-primary" />
        <Label text="{{ message }}" class="h2 text-center" textWrap="true" />
    </StackLayout>
</Page>
